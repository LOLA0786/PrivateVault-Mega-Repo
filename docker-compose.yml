version: '3.8'
services:
  mumbai-protocol:
    build: .
    ports:
      - "8000:8000"
      - "8001:8001"
    volumes:
      - .:/app
    environment:
      - SOVEREIGN_KERNEL_KEY=MUMBAI_FORCE_2026
